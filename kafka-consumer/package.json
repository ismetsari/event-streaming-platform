{
  "name": "kafka-consumer",
  "version": "1.0.0",
  "description": "Kafka consumer that stores events in MongoDB",
  "main": "src/consumer.js",
  "scripts": {
    "start": "node src/consumer.js",
    "docker:build": "docker build -t kafka-event-consumer .",
    "docker:run": "docker run --env-file .env kafka-event-consumer"
  },
  "keywords": ["kafka", "consumer", "mongodb", "events"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kafkajs": "^2.2.4",
    "mongodb": "^6.3.0",
    "dotenv": "^16.3.1"
  }
} 