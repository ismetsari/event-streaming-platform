{
  "name": "event-api",
  "version": "1.0.0",
  "description": "REST API for querying events from MongoDB",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "docker:build": "docker build -t event-api .",
    "docker:run": "docker run -p 3000:3000 --env-file .env event-api"
  },
  "keywords": ["api", "events", "mongodb", "express"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "dotenv": "^16.3.1",
    "morgan": "^1.10.0"
  }
} 